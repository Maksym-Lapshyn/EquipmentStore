@model List<EquipmentStore.Web.Models.OutputViewModel>

@{
	ViewBag.Title = "Готовые решения";
}

<div class="row">
	@foreach (var output in Model)
	{
		string imageSource = null;

		if (output.ImageData != null)
		{
			var base64 = Convert.ToBase64String(output.ImageData.Data);
			imageSource = $"data:{output.ImageData.MimeType};base64,{base64}";
		}

		<div class="col-lg-6 text-center">
			<div class="panel panel-default">
				<div class="panel-heading">
					<h3>@output.Name</h3>
				</div>

				<div class="panel-body">
					<img src="@imageSource" class="img-responsive" alt="@output.Name" />
				</div>

				<div class="panel-footer">
					<p>@output.Description</p>
				</div>
			</div>
		</div>
	}
</div>