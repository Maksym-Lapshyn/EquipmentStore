@model EquipmentStore.Web.Models.ProductSubCategoryViewModel

@{
    ViewBag.Title = "Изменение подкатегории для оборудования";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="panel panel-default">
    @using (Html.BeginForm("Update", "ProductSubCategory", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        <div class="panel-body">
            @Html.HiddenFor(m => m.Id)
            @Html.HiddenFor(m => m.ProductCategoryId)

            <b>
                @Html.LabelFor(m => m.Name)
            </b>
            <p>
                @Html.TextAreaFor(m => m.Name, 2, 1, new { @class = "form-control" })
                @Html.ValidationMessageFor(m => m.Name)
            </p>
        </div>

        <div class="panel-footer">
            <p>
                <button type="submit" class="btn btn-default">Изменить</button>
            </p>

            <p>
                @Html.ActionLink("Назад", "Index", "Admin", null, new { @class = "btn btn-primary" })
            </p>
        </div>
    }
</div>
