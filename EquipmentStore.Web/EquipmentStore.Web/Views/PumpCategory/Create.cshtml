@model EquipmentStore.Web.Models.PumpCategoryViewModel

@{
    ViewBag.Title = "Добавление новой категории для насосов";
}

<div class="panel panel-default">
    @using (Html.BeginForm("Create", "PumpCategory", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        <div class="panel-body">
            @Html.HiddenFor(m => m.Id)

            <b>
                @Html.LabelFor(m => m.Name)
            </b>
            <p>
                @Html.TextAreaFor(m => m.Name, 2, 1, new { @class = "form-control" })
                @Html.ValidationMessageFor(m => m.Name)
            </p>
        </div>

        <div class="panel-footer">
            <p>
                <button type="submit" class="btn btn-default">Добавить</button>
            </p>

            <p>
                @Html.ActionLink("Назад", "Index", "Admin", null, new { @class = "btn btn-primary" })
            </p>
        </div>
    }
</div>
